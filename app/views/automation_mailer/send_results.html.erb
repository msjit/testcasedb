<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
    <styl>
  </head>
  <body>
    <h2>Scheduled Test Plan Execution Complete - <%= @assignment.test_plan.name%></h2>
    
    <p>For more details, please view the results in <%= link_to "TestCaseDB", assignment_url(@assignment) %>.</p>
    
    <table border="0" cellpadding="4" style="text-align: left; background: #999999;">
      <tbody>
        <tr style="background: #303030; color: #ffffff;">
          <th>Test Case</th>
          <th>Product</th>
          <th>Test Case Description</th>
          <th>Notes</th>
          <th>Result</th>
        </tr>
        <% @results.each do |result|%>
          <tr <%= cycle('style="background: #ABC5FB;"', 'style="background: #FFFFFF;"').html_safe %> >
            <td><%= result.test_case.name %></td>
            <td><%= result.test_case.product.name %></td>
        	  <td><%= truncate(result.test_case.description, :length => 70) %></td>
        	  <td>
        	    <% if result.result_statistics %>
        	      <%= generate_result_notes(result).html_safe %>
        	    <% end %>
        	  </td>
        	  <td><%= link_to result.result, result_url(result) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
    
    <br/><br/>
    <p style="font-size: 0.7em;">This email was generated by TestCaseDB. If you no longer wish to receive these emails, please contact your QA department.</p>
  </body>
</html>