<%= fields_for "report" do |f| %>
    <%= f.label :version_id %><br />
    <% if !versions.blank? %>
      <%= f.select :version_id, options_from_collection_for_select(versions, 'id', 'version') %>
    <% else %>
      <%= f.select :version_id, [] %>
	  <% end %>
<% end %>